# DESIGN_TASKS.md — wrks.ai 벤치마킹 디자인 개선 계획

**작성일**: 2026-04-21  
**기준**: wrks.ai 디자인 분석 → WORKON 현재 UI 갭 도출

---

## 가장 많이 보는 페이지 3개

| 순위 | 경로 | 이유 |
|---|---|---|
| 1 | `/` (채팅 메인) | 로그인 후 모든 사용자가 가장 먼저 보는 핵심 화면. ChatInterface + AgentSelector + ConversationSidebar 전체 포함 |
| 2 | `/admin/*` (관리자 패널) | 관리자가 비서/문서/사용자를 매일 운영하는 화면. 좌측 사이드바 포함 |
| 3 | `/employee` (직원 대시보드) | 일반 사용자 진입점. 요약 카드 + 빠른 링크 |

---

## wrks.ai 대비 현재 UI 갭 분석

### 1. 채팅 메인 (`/`)

| 항목 | wrks.ai | 현재 WORKON | 갭 |
|---|---|---|---|
| 전체 폰트 | Pretendard (한국어 최적화) | 시스템 기본 폰트 | 폰트 미설정 |
| 사이드바 배경 | 짙은 네이비 `#1a1f36` | 흰색 `bg-white` | 색상 격차 큼 |
| 에이전트 카드 | 아이콘 + 이름 + 설명 + 색상 포인트 | 작은 원 점 + 이름 + 설명 | 아이콘 없음, 포인트 색 약함 |
| 새 대화 버튼 | 네이비 사이드바 안 밝은 버튼 | `bg-blue-600` 버튼 | 색상 일관성 미흡 |
| 채팅 헤더 | 현재 비서 이름 + 아이콘 + 설정 버튼 | 없음 (AgentSelector가 상단 전체 점유) | 헤더 없음 |
| 포인트 컬러 | `#4f46e5` (인디고/보라) | `bg-slate-900` (검정) | 컬러 아이덴티티 없음 |
| 메시지 버블 | 부드러운 모서리, 아바타 아이콘 | 기본 둥근 박스 | 아바타 없음 |
| 복사/재생성 버튼 | 말풍선 hover 시 노출 | 없음 | 기능 및 UI 모두 없음 |
| 입력창 | 하단 고정, 크게, rounded-xl | 기본 textarea + 전송버튼 | 디자인 차이 큼 |

### 2. 관리자 패널 (`/admin`)

| 항목 | wrks.ai | 현재 WORKON | 갭 |
|---|---|---|---|
| 사이드바 배경 | `#1a1f36` 짙은 네이비 | `bg-blue-900` | 거의 유사, 미세 조정 필요 |
| 메뉴 active 표시 | 좌측 컬러 바 + 배경 | hover만 있고 active 표시 없음 | 현재 페이지 강조 없음 |
| 사이드바 메뉴 아이콘 | SVG 아이콘 (이모지 아님) | 이모지 텍스트 | 이모지 → SVG 교체 필요 |
| 상단 헤더 | 현재 페이지명 + 브레드크럼 | "관리자 패널" 고정 텍스트 | 동적 페이지명 없음 |
| 통계 카드 | 컬러 그라디언트 카드 | 흰색 border 카드 | 시각적 임팩트 부족 |
| 버튼 스타일 | 인디고 포인트 | 검정 (`slate-900`) | 포인트 컬러 미적용 |

### 3. 직원 대시보드 (`/employee`)

| 항목 | wrks.ai | 현재 WORKON | 갭 |
|---|---|---|---|
| 전체 레이아웃 | 좌측 네비게이션 사이드바 | 상단 헤더 수평 nav | 사이드바 구조 없음 |
| 비서 카드 | 이모지/아이콘 + 색상별 구분 | 이름+설명 텍스트만 | 시각적 구분 없음 |
| 빠른 접근 버튼 | 크고 눈에 띄는 카드형 | 작은 텍스트 링크 | 버튼 크기 부족 |

---

## 🎨 디자인 개선 목록 (우선순위 순)

### 🔴 Phase 1 — 체감 효과 최대, 빠른 반영 (1~3일)

- [ ] **전체**: Pretendard 폰트 적용 (`layout.tsx`) — `@fontsource/pretendard` or Google Fonts CDN — **30분**
- [ ] **전체**: 포인트 컬러를 `slate-900(검정)` → `indigo-600 (#4f46e5)`로 교체 (버튼, 링크 강조) — **1시간**
- [ ] **채팅 사이드바**: 배경색 `bg-white` → `#1a1f36` 네이비, 텍스트/버튼 색상 조정 — **1시간**
- [ ] **채팅 상단 헤더**: 에이전트 선택 영역을 compact header로 개선 (선택된 비서 이름 + 아이콘 표시, 변경 버튼) — **2시간**
- [ ] **에이전트 카드**: 이름 첫 글자 아이콘(colored avatar) + 포인트 컬러 배지 추가, 카드 hover shadow 강화 — **1시간**
- [ ] **채팅 입력창**: 하단 고정 영역 padding 확대, textarea `rounded-xl`, 전송 버튼 인디고 색상 적용 — **45분**

### 🟠 Phase 2 — 구조적 UX 개선 (3~5일)

- [ ] **채팅 메시지 버블**: AI 답변에 복사 버튼 추가 (hover 시 표시), 아바타 원형 아이콘 추가 — **2시간**
- [ ] **채팅 메시지 버블**: 재생성 버튼 추가 (AI 메시지 마지막 항목에만 표시) — **3시간**
- [ ] **직원 영역**: Shell 상단 nav → 좌측 사이드바 레이아웃으로 전환 (`/employee` 전용 layout.tsx 생성) — **3시간**
- [ ] **관리자 사이드바**: 현재 페이지 active 강조 (좌측 인디고 바 + 배경색) — **1시간**
- [ ] **관리자 사이드바**: 이모지 → SVG 아이콘으로 교체 (`heroicons` 또는 inline SVG) — **2시간**
- [ ] **관리자 헤더**: 고정 "관리자 패널" → 현재 페이지명 동적 표시 (Next.js `usePathname`) — **1시간**

### 🟡 Phase 3 — 세부 polish (5~7일)

- [ ] **에이전트 카드 (관리자)**: 색상 포인트별 카드 배경, 아이콘 편집 기능 추가 — **3시간**
- [ ] **통계 카드 (관리자 대시보드)**: 그라디언트 배경 카드로 시각적 개선, 트렌드 화살표 추가 — **2시간**
- [ ] **빈 상태(Empty State)**: 에이전트/문서 없을 때 일러스트 또는 안내 문구 + CTA 버튼 — **2시간**
- [ ] **로딩 스피너**: 전체 일관된 로딩 컴포넌트 (Skeleton UI) — **3시간**
- [ ] **다크모드 기반 채팅**: 사이드바 네이비 + 본문 흰색 대비 최적화, 스크롤바 스타일링 — **2시간**
- [ ] **모바일 반응형**: 사이드바 collapse, 햄버거 메뉴 — **4시간**

---

## 예상 총 작업 시간

| Phase | 항목 수 | 예상 시간 |
|---|---|---|
| Phase 1 (즉시 효과) | 6 | ~7시간 |
| Phase 2 (구조 개선) | 6 | ~12시간 |
| Phase 3 (polish) | 6 | ~16시간 |
| **합계** | **18** | **~35시간** |

---

## 시작 권장 순서

```
1. Pretendard 폰트 (30분, 전체 체감 즉시 변화)
2. 포인트 컬러 인디고 교체 (1시간, 버튼 전체 통일)
3. 채팅 사이드바 네이비 배경 (1시간, 가장 큰 시각 변화)
4. 채팅 입력창 개선 + 에이전트 카드 개선 (2시간)
5. 채팅 헤더 compact 재설계 (2시간)
→ Phase 1 완료: wrks.ai 대비 80% 수준 도달 예상
```
