/**
 * 기관 브랜딩 공통 값 (0020).
 *
 * 라우트 파일은 정해진 것 외에 export할 수 없어(Next.js 제약) 상수를 여기 둔다.
 * 클라이언트 훅과 서버 라우트가 같은 값을 쓰게 하려는 목적도 있다 —
 * 양쪽에 따로 적으면 화면과 응답의 문구가 어긋난다.
 */

/** 기관이 따로 지정하지 않았을 때 쓰는 AI 고지 문구. */
export const DEFAULT_AI_NOTICE =
  'AI가 생성한 결과입니다. 사실과 다를 수 있으니 중요한 내용은 원문과 담당자를 통해 확인하세요.';
